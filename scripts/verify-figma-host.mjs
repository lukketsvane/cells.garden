const host=process.argv[2],file=process.argv[3],expected=Number(process.argv[4]);if(!host||!file||!Number.isInteger(expected))throw new Error("usage: node verify-figma-host.mjs <host> <file> <revision>");for(let i=0;i<36;i++){try{const r=await fetch("https://"+host+"/"+file+"?v="+Date.now(),{headers:{"cache-control":"no-cache"}});if(r.ok){const j=await r.json();if(Number(j.revision)===expected){console.log(host+" serves Figma revision "+expected);process.exit(0)}}}catch{}await new Promise(r=>setTimeout(r,5000))}throw new Error(host+" did not serve Figma revision "+expected+" in time");
