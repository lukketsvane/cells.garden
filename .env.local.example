# Local-only tooling. Copy this file to .env.local; .env.local is git-ignored.
# Create a Figma personal access token with file_content:read access to the
# production cells.garden file. Never commit the real token.
FIGMA_TOKEN=
